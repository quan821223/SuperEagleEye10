"""SuperEagleEye runtime package.

Holds the modules that used to live inline in `SuperEagleEye.py`, split by
responsibility. See `doc/detailed-design.md` for the module map and
`doc/<module>.md` for the design notes behind each one.
"""
